APP_URL=http://localhost:5173
# Billing is disabled until a payment provider is configured (Stripe, Razorpay, etc.)
BILLING_ENABLED=false
JWT_SECRET=codeviz-dev-secret-change-in-production
NODE_ENV=development
PORT=8080
CODEVIZ_DATA_DIR=./data

# Stripe (optional — set BILLING_ENABLED=true when ready)
STRIPE_SECRET_KEY=
STRIPE_PRICE_ID=
STRIPE_WEBHOOK_SECRET=
